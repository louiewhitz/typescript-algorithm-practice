function multiply(a: number, b: number) {
    const result = a * b;
    console.log("🚀 ~ file: sol2.ts:3 ~ multiply ~ result:", result);
    return result;
  }
  
  const num1 = 5;
  const num2 = 22;
  
  const result = multiply(num1, num2);
  console.log("Result: ", result);